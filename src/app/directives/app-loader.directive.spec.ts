import { AppLoaderDirective } from './app-loader.directive';

describe('AppLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
